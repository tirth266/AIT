# Flask and Server
flask==3.0.0
flask-cors==4.0.0
flask-socketio==5.3.6
flask-limiter==3.5.0
# eventlet 0.36+ supports Python 3.13; 0.34.x does not
eventlet>=0.36.1
# gevent 24.x has Python 3.13 wheels; 23.x requires Cython compilation that fails on 3.13
gevent>=24.2.1
gevent-websocket>=0.10.1
# gunicorn doesn't run natively on Windows but is kept for Linux/Docker deploys
gunicorn==21.2.0

# Security
bcrypt==4.1.2
pyotp==2.9.0
cryptography>=41.0.7

# Configuration
python-dotenv==1.0.0
PyYAML==6.0.1

# Utilities
python-dateutil==2.8.2
pytz==2023.3
validators==0.22.0
email-validator==2.1.0

# Redis (for caching/sessions)
redis>=5.0.1

# Celery (for async tasks)
celery==5.3.4

# Serialization / validation
marshmallow>=3.20.0

# Data structures
sortedcontainers>=2.4.0

# Logging
python-json-logger==2.0.7

# HTTP requests
requests>=2.31.0
urllib3>=2.1.0

# Data processing
numpy==1.26.4
pandas==2.2.2

# WebSocket client
websocket-client==1.7.0

# Broker Integrations
smartapi-python>=1.3.3
logzero>=1.7.0
loguru>=0.7.2

# System monitoring (for health checks)
psutil>=5.9.6

# Testing
pytest>=7.4.3
pytest-cov>=4.1.0

# Development
watchdog>=3.0.0

# Observability
prometheus-client>=0.19.0

# MongoDB
pymongo[srv]>=4.6.1
dnspython>=2.4.2
